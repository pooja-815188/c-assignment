#include<stdio.h>
int main()
{
    int n=5;
    int arr[]={15,16,6,8,5};
    int i,j,temp;
    for(j=0;j<n-1;j++)
    {
        for(i=0;i<n-1;i++)
        if(arr[i]>arr[i+1])
        {
           temp=arr[i];
           arr[i]=arr[i+1];
           arr[i+1]=temp;
        }
    }
    for(i=0;i<n;i++)
    printf("%d\n",arr[i]);
}
