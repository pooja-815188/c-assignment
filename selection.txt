#include<stdio.h>
int main()
{
    int n=5;
    int arr[]={1,5,2,4,3,6};
    int i,j,temp;
    for(i=0;i<n-1;i++)
    {
        int min=i;
        for(j=i+1;j<n;j++)
        if(arr[j]<arr[min])
        {
            min=j;
        }
    if(min!=i)
    {
        temp=arr[i];
        arr[i]=arr[min];
        arr[min]=temp;
    }
    }
    for(i=0;i<n;i++)
    printf("%d\n",arr[i]);
}
